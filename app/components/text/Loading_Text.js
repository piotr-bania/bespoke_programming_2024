const Loading_Text = ({ title, subtitle }) => {
    return (
        <div className='heading'>
            <h1>{title}</h1>
            <h2>{subtitle}</h2>
        </div>
    )
}

export default Loading_Text