export const canvas_variant = {
    hidden: { opacity: 0 },
    visible: {
        opacity: 1,
        transition: { duration: 3, ease: 'easeInOut' }
    },
    exit: {
        opacity: 0,
        transition: { duration: 3, ease: 'easeInOut' }
    }
}